namespace Resonance.Common
{
    public static class PolicyConstants
    {
        public const string Administration = nameof(Administration);
        public const string Authorized = nameof(Authorized);
        public const string ModifyUserSettings = nameof(ModifyUserSettings);
        public const string Scrobble = nameof(Scrobble);
        public const string Stream = nameof(Stream);
    }
}